import React from "react";
import { ScrollView, View } from "react-native";

const CompanyDetails = (props) => {
  return (
    <ScrollView>
      <View>CompanyDetails</View>
    </ScrollView>
  );
};

export default CompanyDetails;
